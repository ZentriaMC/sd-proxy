---
name: "Build"

on:
  push:
    tags:
      - "20*.*.*"
  pull_request:
    branches:
      - "master"

env:
  REGISTRY: "ghcr.io"
  IMAGE_NAME: "zentriamc/sd-proxy"
  IMAGE_NAME_ECHO: "zentriamc/sd-proxy-echo"
  # github.ref_name is `<pr>/merge` on pull_request events — invalid as a Docker tag. PR builds
  # don't push, so a synthetic `pr-<n>` tag is enough; tag pushes keep the release tag verbatim.
  VERSION: ${{ github.event_name == 'pull_request' && format('pr-{0}', github.event.pull_request.number) || github.ref_name }}

jobs:
  build:
    runs-on: "ubuntu-latest"
    permissions:
      contents: "write"
      packages: "write"
      id-token: "write"
      attestations: "write"

    steps:
      - id: "checkout"
        name: "Checkout repository"
        uses: "actions/checkout@v6"
        with:
          filter: "tree:0"

      - id: "setup-buildx"
        name: "Set up Docker Buildx"
        uses: "docker/setup-buildx-action@v3"

      - id: "login"
        name: "Log in to Container Registry"
        if: ${{ github.event_name != 'pull_request' }}
        uses: "docker/login-action@v3"
        with:
          registry: "${{ env.REGISTRY }}"
          username: "${{ github.actor }}"
          password: "${{ secrets.GITHUB_TOKEN }}"

      - id: "build"
        name: "Build and push Docker image"
        uses: "docker/build-push-action@v6"
        with:
          context: "."
          platforms: linux/amd64,linux/arm64
          push: ${{ github.event_name != 'pull_request' }}
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.VERSION }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
          cache-from: "type=gha"
          cache-to: "type=gha,mode=max"

      - id: "build-echo"
        name: "Build and push proxy-echo test image"
        uses: "docker/build-push-action@v6"
        with:
          context: "."
          target: "proxy-echo"
          platforms: linux/amd64,linux/arm64
          push: ${{ github.event_name != 'pull_request' }}
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME_ECHO }}:${{ env.VERSION }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME_ECHO }}:latest
          cache-from: "type=gha"
          cache-to: "type=gha,mode=max"

      - id: "extract-binaries"
        name: "Extract binaries"
        if: ${{ github.event_name != 'pull_request' }}
        run: |
          binary_name=""
          platform=""
          for arch in amd64 arm64; do
            case "${arch}" in
              amd64)
                binary_name="sd-proxy-x86_64-unknown-linux-musl"
                platform="linux/amd64"
                ;;
              arm64)
                binary_name="sd-proxy-aarch64-unknown-linux-musl"
                platform="linux/arm64"
                ;;
            esac

            docker pull --platform "${platform}" "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.VERSION }}"
            container_id=$(docker container create --platform "${platform}" "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.VERSION }}")
            docker cp "${container_id}":/usr/local/bin/sd-proxy "${binary_name}"
            docker rm "${container_id}"
          done

      - id: "upload-artifacts"
        name: "Upload binary artifacts"
        if: ${{ github.event_name != 'pull_request' }}
        uses: "actions/upload-artifact@v4"
        with:
          name: binaries
          path: sd-proxy-*
          retention-days: 1

      - id: "attestation"
        name: "Generate artifact attestation"
        if: ${{ github.event_name != 'pull_request' }}
        uses: "actions/attest-build-provenance@v3"
        with:
          subject-name: "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}"
          subject-digest: "${{ steps.build.outputs.digest }}"
          push-to-registry: true

      - id: "attestation-echo"
        name: "Generate proxy-echo image attestation"
        if: ${{ github.event_name != 'pull_request' }}
        uses: "actions/attest-build-provenance@v3"
        with:
          subject-name: "${{ env.REGISTRY }}/${{ env.IMAGE_NAME_ECHO }}"
          subject-digest: "${{ steps.build-echo.outputs.digest }}"
          push-to-registry: true

  release:
    needs: build
    if: ${{ github.event_name != 'pull_request' }}
    runs-on: "ubuntu-latest"
    permissions:
      contents: "write"
      id-token: "write"
      attestations: "write"

    steps:
      - id: "download-binaries"
        name: "Download all binaries"
        uses: "actions/download-artifact@v4"
        with:
          name: binaries
          path: .

      - id: "attestation"
        name: "Attest binaries"
        uses: "actions/attest-build-provenance@v3"
        with:
          subject-path: "sd-proxy-*"
          push-to-registry: false

      - id: "create-release"
        name: "Create GitHub Release"
        uses: "softprops/action-gh-release@v2"
        with:
          tag_name: "${{ github.ref_name }}"
          name: "Release ${{ github.ref_name }}"
          files: "sd-proxy-*"
          draft: false
          prerelease: false
